import { ImovelUnidadeTipo } from './imovel-unidade-tipo.model';

describe('ImovelUnidadeTipo', () => {
  it('should create an instance', () => {
    expect(new ImovelUnidadeTipo()).toBeTruthy();
  });
});
